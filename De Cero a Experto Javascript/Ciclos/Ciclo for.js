//con el ciclo for especificamos cuantes veces queremos repetir su bloque de código.

let contador = 0, repeticiones = 100;

for (contador = 0; contador <= repeticiones; contador++ ){
    console.log(contador);
};