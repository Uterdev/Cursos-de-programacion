// Ciclo while solo funciona con valor lógico booleano
// El ciclo while repite una ejecución de bloque de codigo mientras
// la condición sea verdadera

// Imprimir los valores del 0 al 100
let contador = 0, repeticiones = 100;
while(contador <= repeticiones){
    console.log(contador);
    contador++;
}
