// Convertir de cadena a numero en JS
let a ='10', b ='20';
// Si son cadenas se concatenan
console.log(a + b);

// Convertir a numero para que se sumen
let suma = parseInt(a) + parseInt(b);
console.log(suma);
// la funcion integrada en JS "parseInt" convierte un string a numero